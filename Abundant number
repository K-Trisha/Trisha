#include<iostream>
using namespace std;
int main()
{
    int n,sum=0;
    cin>>n;
    for(int i=1;i<=n-1;i++)
    {
        if(n%i==0)
        {
            sum+=i;
        }
    }
    if(n<sum)
    {
        cout<<"Abundant number";
    }
    else
    {
        cout<<"Not Abundant number";
    }
    return 0;
}
